<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>tbcapp</artifactId>
        <groupId>tbc-app</groupId>
        <version>1.0-SNAPSHOT</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>tbc-console</artifactId>
    <name>TBC App Console</name>

    <packaging>war</packaging>
    <properties>
        <gwtVersion>2.6.0</gwtVersion>
        <gxtVersion>3.1.0</gxtVersion>
        <gwtServletVersion>2.6.0</gwtServletVersion>
        <webappDirectory>${project.build.directory}/${project.build.finalName}</webappDirectory>
    </properties>
    <dependencies>
        <dependency>
            <groupId>tbc-app</groupId>
            <artifactId>tbc-services</artifactId>
            <version>1.0-SNAPSHOT</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>com.google.gwt</groupId>
            <artifactId>gwt-servlet</artifactId>
            <version>${gwtServletVersion}</version>
        </dependency>
        <dependency>
            <groupId>com.google.gwt</groupId>
            <artifactId>gwt-user</artifactId>
            <version>${gwtVersion}</version>
        </dependency>
        <dependency>
            <groupId>com.sencha.gxt</groupId>
            <artifactId>gxt</artifactId>
            <version>${gxtVersion}</version>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>2.5.2</version>
        </dependency>
    </dependencies>


    <profiles>
        <profile>
            <id>dev</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>gwt-maven-plugin</artifactId>
                        <version>${gwtVersion}</version>
                        <configuration>
                            <modules>
                                <module>console.appdev</module>
                            </modules>
                            <style>PRETTY</style>
                            <optimizationLevel>0</optimizationLevel>
                            <draftCompile>true</draftCompile>
                            <extraJvmArgs>-Xms1G -Xmx2G</extraJvmArgs>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>org.zeroturnaround</groupId>
                        <artifactId>jrebel-maven-plugin</artifactId>
                        <version>1.1.5</version>
                        <executions>
                            <execution>
                                <id>generate-rebel-xml</id>
                                <phase>process-resources</phase>
                                <goals>
                                    <goal>generate</goal>
                                </goals>
                            </execution>
                        </executions>
                        <configuration>
                            <classpath>
                                <resources>
                                    <resource>
                                        <excludes>
                                            <exclude>console/client/**</exclude>
                                            <exclude>console/*client*/**</exclude>
                                            <exclude>console/*shared*/**</exclude>
                                            <exclude>console/*client/**</exclude>
                                            <exclude>console/*shared/**</exclude>
                                            <exclude>console/zcomp/**</exclude>
                                        </excludes>
                                    </resource>
                                </resources>
                            </classpath>
                            <addResourcesDirToRebelXml>true</addResourcesDirToRebelXml>
                            <alwaysGenerate>true</alwaysGenerate>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
    <build>
        <outputDirectory>${webappDirectory}/WEB-INF/classes</outputDirectory>
        <plugins>
            <plugin>
                <artifactId>maven-war-plugin</artifactId>
                <version>2.1.1</version>
                <configuration>
                    <packagingExcludes>WEB-INF/web.xml</packagingExcludes>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>gwt-maven-plugin</artifactId>
                <version>${gwtVersion}</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>compile</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <debugSuspend>false</debugSuspend>
                    <source>${maven.compiler.source}</source>
                    <target>${maven.compiler.target}</target>
                    <modules>
                        <module>console.app</module>
                    </modules>

                    <encoding>UTF-8</encoding>
                    <hostedWebapp>${webappDirectory}</hostedWebapp>
                    <noServer>true</noServer>
                    <runTarget>tbcapp-console/app.html</runTarget>
                    <port>8080</port>
                    <generateDirectory>${project.build.directory}/generated-sources/gwt</generateDirectory>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>